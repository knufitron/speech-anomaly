name: random_forest

# P(anomaly) cutoff for hard labels when y_score is in [0, 1]; null = use `predict()`.
probability_threshold: null

params:
  n_estimators: 200
  max_depth: 10
  # class_weight: balanced_subsample
  class_weight:
    0: 0.1
    1: 0.9
  random_state: 42
  n_jobs: -1
