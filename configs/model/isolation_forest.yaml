name: isolation_forest

# Unsupervised scores are usually not probabilities; typically ignored unless y_score in [0, 1].
probability_threshold: null

params:
  n_estimators: 200
  contamination: auto
  random_state: 42
  n_jobs: -1
